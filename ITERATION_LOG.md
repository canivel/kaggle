- **★★ ENGINE-SWAP EVAL `canivel/arc3-q38-engine-eval` v1 PUSHED (08-15 slot 2) + RUNNING.** Verification **survived and was not inherited** — agent pulled all three mirrors and read the **pinned vLLM 0.19.0 wheel + transformers 4.57.6 wheel**, not docs. All three mirrors **byte-identical on all 7 config/tokenizer/template files**, **none missing a file** (the 122B's post-78 GB-load failure mode does not recur). `text_config` **identical in all 33 fields**; only 7 differences anywhere, each run to ground: (1) **`vision_config.model_type` changed** (`qwen3_5_vision`→`qwen3_5`) — **the research report MISSED this**; harmless because vLLM builds the vision config via `sub_configs`, never a `model_type` lookup, and the new value *matches vLLM's own class attribute, which the incumbent's did not*; (2) root `dtype` absent — `get_torch_dtype` falls back to `text_config.dtype='bfloat16'` before the fp16 default; (3) blockwise FP8 on SM120 falls through CUTLASS→aiter→**Triton** ⇒ **degrades, does not raise** — structurally unlike the 122B's MoE-only FlashInfer JIT death, which a dense 27B never enters; (4) `Qwen2VLImageProcessorFast` + `TRANSFORMERS_NO_TORCHVISION=1` handled by 4.57.6's strip-"Fast" fallback. Bonus: the new `tokenizer_config.json` is **transformers-v4 layout — CLOSER to our pinned 4.57.6 than the incumbent's v5**. Mirror chosen `saltb0x/qwen3-8-27b-fp8` on operational risk only (most downloads, Apache-2.0 vs `unknown`); `mustangliu` is a same-bytes fallback.
  - **★ `reasoning_effort` PINNED TO `medium` — MEASURED, NOT ARGUED.** Reading the template: `xhigh` (default) and `low` each inject an instruction paragraph; **`medium` is validated-but-silent.** Rendered on a harness-shaped payload: **Qwen3.6 = 1495 chars, Qwen3.8@medium = 1495 chars, ZERO-LINE DIFF**; @xhigh 1704, @low 1633. **The only value that reproduces our current prompt byte-for-byte.** Verified in the wheel that the pin reaches the template (`engine/serving.py:807` merges default|request kwargs; harness only sends `enable_thinking`). xhigh/low = separate later arm.
  - Gates: **smoke 81/0, scorer selftest 22/0, preflight ALLOW 0/0, D4=[2,6,8] EXACT**, pull-back **3/3 dataset_sources survived incl. the 25 GB engine**, env byte-identical to the frozen fork; cells 12/14 (the whole measurement surface) byte-identical; footprint on the setup command = **4 removed lines with 18 invariants asserted**. **Every gate has a PAIRED NEGATIVE CONTROL** (rejects a staged real Qwen3.6 snapshot; rejects a missing `tokenizer_config.json` BEFORE the load; rejects 63 shards; AST gate catches an injected out-of-scope call — the exact class that killed LoRA v1).
  - **★★ NEW CAMPAIGN-LEVEL DEFECT, settled by experiment not inference: THE KAGGLE CLI PUSH PATH IS NOT BYTE-TRANSPARENT — it re-encodes non-ASCII UTF-8→cp1252.** Pulling the *upstream public* notebook returns clean U+2014, so the **push** side mangles. **This retroactively explains weeks of "em-dash round-trip mojibake" we had attributed to the pull.** Only load-bearing casualty: the BPE-space `.replace()` in the server-side effort probe stops matching, so it correctly declares itself **BLIND and WARNs** rather than raising; the pin stays certified by the pure-ASCII local render with `FATAL if neither instrument certifies` intact. **Ruled: v1 stands, no v2** (a cosmetic re-push would spend a slot we don't have). Banked: builder now emits `\uXXXX` escapes, smoke gates "cells 2/6/8 must be pure ASCII", push script one-shot guard (exit 5).
  - **SEALED READ** (baseline `duck-harness-kaggle` m=3: lc **18/19/21** = 0.7733/game; σ̂ 0.14174 df 6): **CONFIRM-2× ≥ 32 levels · REFUTE-2× ≤ 25 · HARM ≤ 12 · third state INFRA DEATH.** **95.3% power** against a true doubling; REFUTE rejects the "consistent 2×" claim at **3.2σ**. Score-based reading declared **non-inferential in advance** (baseline mean_score 1.427/1.939/3.420, sd 1.033, n=3, ~60% power). Secondary: tok/s vs baseline 212.52/203.66/197.47 — **a 25.3 GB model SHOULD be faster; if it isn't, that is the Triton-fallback signature.** Committed `8132502`.
